@model IEnumerable<Entities.Concrete.Place>

@{
    ViewData["Title"] = "Bölge Listesi | Tour Company";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Bölge Listesi</h1>

<p>
    <button type="button" class="btn btn-primary" onclick="openCreateModal()">Yeni Bölge Tanım</button>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Bölge Adı</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <button type="button" class="btn btn-warning" onclick="openEditModal(@item.PlaceId)">Düzenle</button>
                    <button type="button" class="btn btn-info" onclick="openDetailModal(@item.PlaceId)">Detay</button>
                    <button type="button" class="btn btn-danger" onclick="openDeleteModal(@item.PlaceId)">Sil</button>
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="staticBackdropLabel"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
            </div>
            <div class="modal-footer">
            </div>
        </div>
    </div>
</div>


@section Scripts{
    <script src="~/js/pages/placeModul.js" asp-append-version="true"></script>
}